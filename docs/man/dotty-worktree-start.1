.nh
.TH "DOTTY" "1" "Jun 2026" "Auto generated by spf13/cobra" ""

.SH NAME
dotty-worktree-start - Create or reuse an agent/* worktree and print its path.


.SH SYNOPSIS
\fBdotty worktree start [repo] [suffix] [flags]\fP


.SH DESCRIPTION
Create (or reuse) a worktree for repo on an agent/- branch at
the configured worktree location, and print its path — the path is the
command's result, so hooks and scripts capture stdout.

.PP
repo falls back to $CLAUDE_PROJECT_DIR, then the enclosing repository.
suffix falls back to the WorktreeCreate hook JSON on stdin ({"name": ...}),
then a UTC timestamp.


.SH OPTIONS
\fB-h\fP, \fB--help\fP[=false]
	help for start


.SH OPTIONS INHERITED FROM PARENT COMMANDS
\fB--profile\fP=""
	profile to operate on (defaults to the active profile)


.SH EXAMPLE
.EX
  dotty worktree start
  dotty worktree start ~/Repos/dotty fix-linker
  echo '{"name":"fix-1"}' | dotty worktree start   # hook form
.EE


.SH SEE ALSO
\fBdotty-worktree(1)\fP
