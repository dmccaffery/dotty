# Agent instructions

## Go code style

Always use the `/go-style` skill (if available) when making edits to Go code
in this repository — load it before writing or modifying any `.go` file so
the edits follow its conventions (error wrapping, logging, interfaces,
cobra/viper patterns).
